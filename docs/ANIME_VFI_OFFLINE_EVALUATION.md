# Anime VFI offline evaluation

Date: 2026-09-03

Status: one host comparison and one bounded physical-device native probe completed. This is not a
player integration, realtime claim, visual-quality approval, sustained thermal result, or model
redistribution approval.

## Outcome

Only RIFE ncnn Vulkan v4.6 advances as the current offline mobile probe. IFRNet-S remains a host
comparison. The decision is narrow: on the same project-generated 320x180 line-art motion pair,
RIFE produced a substantially better ground-truth proxy result, while IFRNet-S was smaller and
faster. Neither result is evidence for representative anime or normal playback.

No file under `app/` changed. `QuickSrVideoEffect`, Media3 frame ownership, QNN queueing, cadence
reuse, and Anime4K remain authoritative and unchanged. VFI is still an optional smooth-mode
experiment whose default state is absent/off.

## Candidate and rights audit

| Candidate | Source identity | Code license | Selected model | Model bytes | Weight rights | Result |
| --- | --- | --- | --- | ---: | --- | --- |
| RIFE ncnn Vulkan | [`a7532fc`](https://github.com/nihui/rife-ncnn-vulkan/commit/a7532fc3f9f8f008cd6eecd6f2ffe2a9698e0cf7) | MIT | `rife-v4.6` | 10,631,069 | No independent weight license statement found; redistribution blocked | Advanced to standalone Android CLI probe |
| IFRNet ncnn Vulkan | [`3592a70`](https://github.com/nihui/ifrnet-ncnn-vulkan/commit/3592a70355ec011fe7cefb3a9ba08b63d82a2b6d) | MIT | `IFRNet_S_Vimeo90K` | 5,935,644 | No independent weight license statement found; redistribution blocked | Host comparison only |

The research sources were refreshed at RIFE commit
[`5d8adbd`](https://github.com/hzwer/ECCV2022-RIFE/commit/5d8adbdd40e12c2c8f91930eff838aebe561c086)
and IFRNet commit
[`b117bca`](https://github.com/ltkong218/IFRNet/commit/b117bcafcf074b2de756b882f8a6ca02c3169bfe).
Both research repositories also expose MIT licenses. That does not close the transformed-weight
provenance gap: a downloadable model in a repository or release is not treated here as an explicit
grant to redistribute the model.

The RIFE port's latest release is 2022-10-29 and its compact v4.6 model predates the upstream
RIFE v4.7-v4.10 anime-optimization notice. The older port model named `rife-anime` maps to upstream
1.8 and is about 60 MB across three networks, so it was not silently substituted for v4.6. The
IFRNet port explicitly calls itself early development, has three commits, and provides Vimeo90K or
GoPro models rather than an anime-domain model.

The complete file hashes, release asset hashes, source links, operator lists, and blocked rights
states are in `vfi-benchmark/candidates.json`. RIFE v4.6 uses
convolution/deconvolution, interpolation, pixel shuffle, binary/elementwise operations, crop,
concat/split, sigmoid/ReLU, and a custom warp layer. IFRNet-S uses a similar convolutional graph
with PReLU, slice, interpolation, and its custom warp layer.

## Decision contract

`VfiPrefilter` in `vfi-benchmark/vfi_prefilter.py` consumes decoded frames in presentation order:

1. no prior frame, stream-epoch change, or generation change: `BYPASS` and replace the anchor;
2. exact identity or bounded near-hold difference: `BYPASS`;
3. high whole-frame change meeting both cut thresholds: `BYPASS`;
4. otherwise: `INTERPOLATE / DISTINCT_DRAWING`.

This makes fixed odd/even selection and blind interpolation impossible in the offline runner. The
project-generated fixture has 12 events: one true distinct pair interpolates, while the first
frame, exact hold, near hold, hard cut, seek generation change, and stream-epoch changes all bypass.
The fixture manifest SHA-256 is
`2e829049491b35e4838a7ed7a657b999d8f9b37064f17af0ec6c457e6deb90af`.

## Host comparison

The host metric is the full upstream subprocess wall time, including model/runtime startup and PNG
I/O. It is not an inner-kernel timing.

| Candidate | Three wall times | Peak private bytes observed | PSNR | Global SSIM proxy | Edge-gradient MAE |
| --- | --- | ---: | ---: | ---: | ---: |
| RIFE v4.6 | 758.99 / 749.83 / 735.96 ms | 301,469,696 | 27.120 dB | 0.983676 | 0.009356 |
| IFRNet-S Vimeo90K | 567.04 / 541.58 / 544.35 ms | 127,438,848 | 16.604 dB | 0.798559 | 0.015838 |

The proxy target is a known midpoint generated by the same fixture script. LPIPS is unavailable in
the dependency-minimal runner, and human review remains pending. These numbers select a probe; they
do not establish visual superiority across anime.

## Adreno 740 bounded probe

The upstream RIFE source and pinned ncnn/libwebp submodules were cross-compiled as a standalone
arm64 CLI with Android NDK 25.2.9519653. A six-line reproducible timing patch measures only
`RIFE::process`; model loading, Vulkan startup, PNG decode/encode, adb, and process startup remain in
the separate end-to-end measurement. The binary was not packaged into an APK.

Observed device: `2304FPN6DC / ishtar`, SM8550, Android 16 API 36, Adreno 740. All pushed files were
SHA-256 checked again on device.

| Measurement | Observed value |
| --- | --- |
| Instrumented binary SHA-256 | `974e1b71827da1509948fd000f6ed352fda005bc64e377ec574e80920d87515b` |
| Model wall times | 104.719 / 102.391 / 103.330 ms |
| Unpolled latency median | 102.860 ms (last two runs; first run sampled memory) |
| Whole-process wall times | 1651.646 / 1761.574 / 1655.936 ms |
| Sampled peak PSS / RSS | 186,825 / 228,440 kB |
| Maximum thermal-zone proxy | 44.383 C before, 48.4 C after |
| Output identity | stable across runs; SHA-256 `efa060db25c959105753fe3161957e6fdaaf928a288d2de6a058c2e1f4886c83` |
| Output quality proxies | 27.311 dB PSNR, 0.984384 global SSIM, 0.009211 edge-gradient MAE |
| Lifecycle | process absent after bounded runs |

The approximately 103 ms model time is already above 24 fps and 30 fps frame budgets before
player scheduling, decode, composition, A/V sync, and sustained thermal cost. The approximately
1.7 second process time is dominated by a fresh CLI process and is not a resident-runtime estimate.

## Reproduction and publication boundary

`vfi-benchmark/README.md` provides host commands. The Android build
script verifies the upstream and submodule commits, applies the timing patch, and builds only the
native probe. The Android runner verifies one connected serial, remote file hashes, PSS/RSS,
temperature proxies, model and end-to-end wall time, outputs, and process exit.

Committed files contain only source, links, hashes, manifests, tests, sanitized measurements, and
this report. Source/model clones, release archives, weights, binaries, PNGs, outputs, device reports,
and remote evidence stay below ignored `local-artifacts/` or `/data/local/tmp`. No push is part of
this task.

## Still unproven

- representative rights-clear anime clips, difficult occlusion, pans, subtitles, line boil, and
  more than one known synthetic midpoint;
- LPIPS, warp/flicker metrics, and human visual review;
- resident Android runtime startup, buffer ownership, scheduling, A/V sync, seek/flush integration,
  sustained 10-30 minute temperature/power, or a safe frame-rate policy;
- compatibility outside the single observed device/firmware/driver;
- model-weight redistribution rights.

Therefore the mergeable core is limited to the prefilter/decision contract, reproducible fixture,
candidate/rights manifests, standalone host/device probe tooling, and evidence report. Integrating
VFI into the default or optional player path requires a separate review after the open gates above.
